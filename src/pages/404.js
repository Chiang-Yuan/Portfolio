import React from 'react';
import Box from 'components/box';
import Layout from 'components/layout';

const NotFound = () => (
  <Layout>
    <Box>Ooops! Yuan is busy constructing this website.</Box>
  </Layout>
);

export default NotFound;
